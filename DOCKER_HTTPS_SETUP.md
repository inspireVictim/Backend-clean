# 🐳 Настройка HTTPS для Docker

## ✅ Что уже настроено автоматически

- ✅ `Program.cs` - полная поддержка HTTPS
- ✅ `docker-compose.yml` - правильный проброс портов (8000:5000, 8443:5001)
- ✅ `Dockerfile` - правильные порты экспозиции
- ✅ Скрипты создания сертификата

## 📋 Пошаговая инструкция

### Шаг 1: Создайте SSL сертификат

#### На Linux/Mac:

```bash
# В директории проекта
chmod +x setup_cert_for_docker.sh
./setup_cert_for_docker.sh
```

#### На Windows:

```powershell
# В PowerShell в директории проекта
.\setup_cert_for_docker.ps1
```

Скрипт создаст:
- 📁 `./certs/yess-cert.pfx` - сертификат для приложения
- 🔑 Пароль: `YesSGo!@#!`

### Шаг 2: Активируйте HTTPS в docker-compose.yml

Откройте `docker-compose.yml` и раскомментируйте строки:

```yaml
environment:
  # Раскомментируйте эти 2 строки:
  - ASPNETCORE_KESTREL__CERTIFICATES__DEFAULT__PATH=/etc/ssl/certs/yess-cert.pfx
  - ASPNETCORE_KESTREL__CERTIFICATES__DEFAULT__PASSWORD=YesSGo!@#!

volumes:
  # Раскомментируйте эту строку:
  - ./certs:/etc/ssl/certs:ro
```

**Важно:** `:ro` означает "read-only" - контейнер не сможет изменять сертификаты.

### Шаг 3: Откройте порты в firewall (на сервере)

```bash
# Для UFW
sudo ufw allow 8443/tcp
sudo ufw allow 8000/tcp

# Или для firewalld
sudo firewall-cmd --permanent --add-port=8443/tcp
sudo firewall-cmd --permanent --add-port=8000/tcp
sudo firewall-cmd --reload
```

### Шаг 4: Перезапустите Docker контейнеры

```bash
# Остановите контейнеры
docker-compose down

# Пересоберите и запустите
docker-compose up -d --build

# Или если не нужно пересобирать:
docker-compose up -d
```

### Шаг 5: Проверьте работу

```bash
# Проверка HTTP
curl http://localhost:8000/health
# или
curl http://5.59.232.211:8000/health

# Проверка HTTPS
curl -vk https://localhost:8443/health
# или
curl -vk https://5.59.232.211:8443/health

# Проверка логов
docker-compose logs -f csharp-backend
```

В логах вы должны увидеть:
- ✅ `HTTP настроен на порту 5000 для обратного прокси`
- ✅ `HTTPS настроен для Production на порту 5001 с сертификатом...`
- ✅ `HTTPS Redirection и HSTS включены`

## 🔧 Структура файлов

```
yess-backend-dotnet/
├── certs/                          # Директория для сертификатов (создаётся автоматически)
│   ├── yess-cert.pfx              # Сертификат (добавьте в .gitignore!)
│   ├── yess-cert.pem
│   └── yess-cert-key.pem
├── docker-compose.yml              # Конфигурация Docker
├── Dockerfile                      # Образ приложения
├── setup_cert_for_docker.sh       # Скрипт создания сертификата (Linux/Mac)
├── setup_cert_for_docker.ps1      # Скрипт создания сертификата (Windows)
└── DOCKER_HTTPS_SETUP.md          # Эта инструкция
```

## 📝 Пример готового docker-compose.yml

```yaml
csharp-backend:
  # ... другие настройки ...
  ports:
    - "8000:5000"   # HTTP
    - "8443:5001"   # HTTPS
  environment:
    - ASPNETCORE_ENVIRONMENT=Production
    # ... другие переменные ...
    # HTTPS Certificate:
    - ASPNETCORE_KESTREL__CERTIFICATES__DEFAULT__PATH=/etc/ssl/certs/yess-cert.pfx
    - ASPNETCORE_KESTREL__CERTIFICATES__DEFAULT__PASSWORD=YesSGo!@#!
  volumes:
    - ./certs:/etc/ssl/certs:ro
    - ./uploads:/app/uploads
```

## 🔍 Проверка портов в контейнере

```bash
# Зайдите в контейнер
docker exec -it csharp-backend bash

# Проверьте порты
netstat -tlnp | grep -E '(5000|5001)'
# или
ss -tlnp | grep -E '(5000|5001)'

# Проверьте наличие сертификата
ls -la /etc/ssl/certs/yess-cert.pfx

# Выйдите
exit
```

## 🆘 Решение проблем

### Проблема: Порт 8443 не отвечает

**Решение:**
1. Проверьте логи: `docker-compose logs csharp-backend`
2. Убедитесь, что сертификат создан: `ls -la ./certs/yess-cert.pfx`
3. Убедитесь, что volume смонтирован: `docker exec csharp-backend ls /etc/ssl/certs/`
4. Проверьте, что переменные окружения заданы: `docker exec csharp-backend env | grep CERT`

### Проблема: "Certificate not found" в логах

**Решение:**
1. Проверьте путь к сертификату в volume: `docker exec csharp-backend ls -la /etc/ssl/certs/`
2. Убедитесь, что volume правильно смонтирован в `docker-compose.yml`
3. Перезапустите контейнер: `docker-compose restart csharp-backend`

### Проблема: "CryptographicException" в логах

**Решение:**
1. Проверьте пароль в переменной окружения `ASPNETCORE_KESTREL__CERTIFICATES__DEFAULT__PASSWORD`
2. Пересоздайте сертификат: удалите `./certs/yess-cert.pfx` и запустите скрипт снова
3. Убедитесь, что сертификат не повреждён: `openssl pkcs12 -info -in ./certs/yess-cert.pfx`

### Проблема: Контейнер не запускается

**Решение:**
1. Проверьте логи: `docker-compose logs csharp-backend`
2. Попробуйте запустить без HTTPS (закомментируйте переменные окружения для сертификата)
3. Проверьте синтаксис `docker-compose.yml`: `docker-compose config`

## 🔒 Безопасность

1. **Не коммитьте сертификаты в Git!**
   - Добавьте `certs/` в `.gitignore`
   - Создавайте сертификат на каждом сервере отдельно

2. **Используйте переменные окружения для пароля:**
   - Можно вынести пароль в `.env` файл
   - Или использовать Docker secrets в production

3. **Самоподписанный сертификат** подходит только для тестирования
   - Для production используйте Let's Encrypt или сертификат от CA

## 📋 Чеклист

- [ ] Сертификат создан: `./certs/yess-cert.pfx`
- [ ] Переменные окружения раскомментированы в `docker-compose.yml`
- [ ] Volume для сертификатов раскомментирован в `docker-compose.yml`
- [ ] Порты открыты в firewall (8000, 8443)
- [ ] Контейнеры перезапущены: `docker-compose up -d`
- [ ] HTTP работает: `curl http://localhost:8000/health`
- [ ] HTTPS работает: `curl -vk https://localhost:8443/health`
- [ ] Логи показывают успешную настройку HTTPS

## 🎯 Быстрая команда для проверки

```bash
# Полная проверка
echo "🔍 Проверка HTTP..."
curl -s http://localhost:8000/health | head -1

echo "🔍 Проверка HTTPS..."
curl -sk https://localhost:8443/health | head -1

echo "🔍 Проверка портов в контейнере..."
docker exec csharp-backend netstat -tlnp 2>/dev/null | grep -E '(5000|5001)' || \
docker exec csharp-backend ss -tlnp 2>/dev/null | grep -E '(5000|5001)'

echo "🔍 Проверка сертификата в контейнере..."
docker exec csharp-backend ls -la /etc/ssl/certs/yess-cert.pfx
```

## 🎉 Готово!

После выполнения всех шагов:
- ✅ HTTP доступен на порту **8000**
- ✅ HTTPS доступен на порту **8443**
- ✅ Swagger доступен на `/docs`
- ✅ Приложение работает в Docker с HTTPS поддержкой

