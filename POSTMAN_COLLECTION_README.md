# 📬 Инструкция по использованию коллекции Postman

## 📥 Как импортировать коллекцию в Postman

### Вариант 1: Импорт из файла
1. Откройте Postman
2. Нажмите **Import** (левый верхний угол)
3. Выберите файл `YESS_BACKEND_POSTMAN_COLLECTION.json`
4. Нажмите **Import**
5. Готово! Коллекция появится в списке слева

### Вариант 2: Импорт через URL (если файл в GitHub/GitLab)
1. Откройте Postman
2. Нажмите **Import**
3. Вкладка **Link**
4. Вставьте URL файла
5. Нажмите **Continue** → **Import**

---

## ⚙️ Настройка переменных

После импорта проверьте переменные коллекции:

1. Кликните на коллекцию **"Yess Backend - Diagnostics & API"**
2. Перейдите на вкладку **Variables**
3. Убедитесь, что переменные настроены:

| Переменная | Значение | Описание |
|------------|----------|----------|
| `base_url` | `http://5.59.232.211:8000` | ⚠️ **СЕРВЕР** - не localhost! |
| `api_version` | `v1` | Версия API |
| `access_token` | *(пусто)* | Автоматически заполняется при входе |
| `refresh_token` | *(пусто)* | Автоматически заполняется при входе |
| `verification_code` | *(пусто)* | Автоматически заполняется при отправке кода |

---

## 🚀 Быстрый старт

### Шаг 1: Проверка сервера
1. Откройте **"1. Diagnostics"** → **"GET - Root (Basic)"**
2. Нажмите **Send**
3. Должен вернуться ответ `200 OK` с информацией о сервере

### Шаг 2: Диагностический POST запрос
1. Откройте **"1. Diagnostics"** → **"POST - Send Verification Code (Диагностика)"**
2. Нажмите **Send**
3. Проверьте:
   - ✅ Статус `200 OK`
   - ✅ В тестах нет ошибок
   - ✅ В Console видно "✅ POST на СЕРВЕР работает!"

### Шаг 3: Регистрация пользователя
1. Откройте **"2. Authentication"** → **"POST - Send Verification Code"**
2. Нажмите **Send** (код сохранится в переменную)
3. Откройте **"POST - Verify Code and Register"**
4. Код уже подставлен из шага 2
5. Нажмите **Send**

### Шаг 4: Вход
1. Откройте **"POST - Login"**
2. Укажите правильный пароль
3. Нажмите **Send**
4. Токены автоматически сохранятся

---

## 🔍 Автоматическое сохранение данных

Коллекция автоматически сохраняет:
- ✅ **Код верификации** → в `verification_code` (после отправки кода)
- ✅ **Access токен** → в `access_token` (после входа)
- ✅ **Refresh токен** → в `refresh_token` (после входа)

После входа все защищенные запросы автоматически используют токен из переменной `{{access_token}}`.

---

## ✅ Как понять, что запрос идет на сервер, а не localhost?

### В Postman:
1. Откройте любой запрос
2. Посмотрите на URL вверху
3. Должно быть: `http://5.59.232.211:8000/...`
4. **НЕ должно быть**: `localhost`, `127.0.0.1`, `10.0.2.2`

### В тестах:
После выполнения запроса **"POST - Send Verification Code (Диагностика)"** проверьте:
- ✅ Тест "URL contains server IP" - должен пройти
- ✅ Тест "URL does NOT contain localhost" - должен пройти

---

## 🐛 Диагностика проблем

### Проблема: "Could not get any response"
**Решение:**
- Проверьте, что сервер запущен: `docker-compose ps`
- Проверьте URL: должен быть `5.59.232.211:8000`
- Проверьте доступность сервера в браузере: `http://5.59.232.211:8000/health`

### Проблема: "401 Unauthorized"
**Решение:**
- Выполните запрос **"POST - Login"** сначала
- Проверьте, что токен сохранился в переменной `access_token`
- Обновите запрос (токен подставится автоматически)

### Проблема: "400 Bad Request"
**Решение:**
- Проверьте формат данных в Body
- Проверьте обязательные поля
- Посмотрите на сообщение об ошибке в ответе

### Проблема: "500 Internal Server Error"
**Решение:**
- Это проблема на сервере
- Проверьте логи: `docker-compose logs csharp-backend`
- Проверьте подключение к БД

---

## 📝 Изменение адреса сервера

Если нужно поменять IP сервера:

1. Откройте коллекцию
2. Перейдите на вкладку **Variables**
3. Измените значение `base_url` на новый адрес
4. Все запросы автоматически будут использовать новый адрес

**Пример:**
- Было: `http://5.59.232.211:8000`
- Стало: `http://192.168.1.100:8000`

---

## 🔐 Запросы, требующие авторизации

Эти запросы требуют токен в заголовке `Authorization`:
- `GET - Get Current User (Me)`
- `GET - Get Balance`
- `GET - Get Wallet`
- `GET - Get Transactions`
- `POST - Calculate Order`
- `GET - Get User Orders`

**Токен автоматически подставляется** из переменной `{{access_token}}`.

**Чтобы получить токен:**
1. Выполните запрос **"POST - Login"**
2. Токен сохранится автоматически
3. Все последующие запросы будут работать

---

## 📚 Дополнительная информация

- **База URL:** `http://5.59.232.211:8000`
- **Версия API:** `v1`
- **Полный путь:** `http://5.59.232.211:8000/api/v1/...`

---

## ✅ Чеклист перед тестированием

- [ ] Сервер запущен и доступен
- [ ] Коллекция импортирована в Postman
- [ ] Переменная `base_url` = `http://5.59.232.211:8000`
- [ ] Выполнен тест "GET - Root (Basic)" - работает
- [ ] Выполнен тест "POST - Send Verification Code (Диагностика)" - работает

**Готово к тестированию!** 🚀

