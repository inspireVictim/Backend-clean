# ⚡ Быстрый тест бэкенда

## 🎯 За 30 секунд проверить, работает ли бэкенд

### Вариант 1: Через браузер (самый простой)

Откройте в браузере:
```
http://5.59.232.211:8000/health
```

**Ожидаемый результат:**
```json
{
    "status": "healthy",
    "service": "yess-backend",
    "timestamp": "2024-01-15T10:30:00Z"
}
```

✅ **Если видите этот ответ** → бэкенд работает!

---

### Вариант 2: Через Postman (рекомендуется)

1. Откройте Postman
2. Создайте новый запрос:
   - **Method:** `GET`
   - **URL:** `http://5.59.232.211:8000/api/v1/health`
3. Нажмите **Send**

**Ожидаемый результат:** `200 OK` с информацией о БД и Redis

✅ **Если `200 OK`** → бэкенд работает и БД подключена!

---

### Вариант 3: Через curl (в командной строке)

```bash
curl http://5.59.232.211:8000/health
```

Или для полной проверки:
```bash
curl http://5.59.232.211:8000/api/v1/health
```

---

## 🔍 POST запрос для диагностики

### Через Postman:

**POST** `http://5.59.232.211:8000/api/v1/auth/send-verification-code`

**Headers:**
```
Content-Type: application/json
```

**Body (raw JSON):**
```json
{
    "phone_number": "+996700123456"
}
```

**Ожидаемый результат:**
```json
{
    "phone_number": "+996700123456",
    "code": "123456",
    "verification_code": "123456",
    "message": "Код отправлен (development mode)",
    "success": true
}
```

✅ **Если видите этот ответ** → POST запросы работают!

---

## 🚨 Если не работает

### Ошибка: "Could not get any response"
- Сервер не запущен или недоступен
- Проверьте: `docker-compose ps`
- Запустите: `docker-compose up -d`

### Ошибка: "Connection refused"
- Сервер недоступен по этому IP
- Проверьте файрвол
- Проверьте, что сервер слушает на правильном порту

### Ошибка: "500 Internal Server Error"
- Проблема в бэкенде
- Проверьте логи: `docker-compose logs csharp-backend`

---

## ✅ Результат

- **200 OK** → Всё работает, можно тестировать дальше
- **400/500** → Бэкенд работает, но есть ошибки (смотрите логи)
- **Нет ответа** → Сервер недоступен (проверьте запуск)

---

**Для полного тестирования используйте коллекцию Postman: `YESS_BACKEND_POSTMAN_COLLECTION.json`**

